var firstName = "Jennifer";
var secondName = "Richard";

let firstNameLength = firstName.length;
let secondNameLength = secondName.length;

var characterAmount = firstNameLength - secondNameLength;

console.log(
  `The name ${firstName} is longer than the name ${secondName} by ${characterAmount} character(s)`
);
